# Configuration template for Warehouse Management System (WMS)
apiVersion: hsaaiv2/v1
kind: IntegrationConfig
metadata:
  name: wms-config
  labels:
    integration: INT-WMS
    category: manufacturing
    vendor: Multiple
spec:
  enabled: true
  endpoint: "${INT-WMS_ENDPOINT}"
  credentials:
    source: vault
    path: "secret/hsaai/integrations/wms"
  auth:
    type: oauth2
    clientId: "${INT-WMS_CLIENT_ID}"
    clientSecret: "vault:wms/client-secret"
    tenant: "${INT-WMS_TENANT}"
  pool:
    maxConnections: 20
    idleTimeout: 300s
  retry:
    maxRetries: 3
    backoff: exponential
    initialDelay: 1s
    maxDelay: 30s
  circuitBreaker:
    enabled: true
    failureThreshold: 5
    recoveryTime: 60s
  rateLimit:
    requestsPerSecond: 50
    burst: 100
  observability:
    metricsEnabled: true
    logsEnabled: true
    tracesEnabled: true
    logLevel: info
  security:
    piiDetection: true
    auditAll: true
    tlsMinVersion: "1.3"
