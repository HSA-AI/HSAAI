# Configuration template for Azure Active Directory (Entra ID)
apiVersion: hsaaiv2/v1
kind: IntegrationConfig
metadata:
  name: azure-ad-config
  labels:
    integration: INT-AZURE-AD
    category: identity
    vendor: Microsoft
spec:
  enabled: true
  endpoint: "${INT-AZURE-AD_ENDPOINT}"
  credentials:
    source: vault
    path: "secret/hsaai/integrations/azure-ad"
  auth:
    type: oauth2
    clientId: "${INT-AZURE-AD_CLIENT_ID}"
    clientSecret: "vault:azure-ad/client-secret"
    tenant: "${INT-AZURE-AD_TENANT}"
  pool:
    maxConnections: 20
    idleTimeout: 300s
  retry:
    maxRetries: 3
    backoff: exponential
    initialDelay: 1s
    maxDelay: 30s
  circuitBreaker:
    enabled: true
    failureThreshold: 5
    recoveryTime: 60s
  rateLimit:
    requestsPerSecond: 50
    burst: 100
  observability:
    metricsEnabled: true
    logsEnabled: true
    tracesEnabled: true
    logLevel: info
  security:
    piiDetection: true
    auditAll: true
    tlsMinVersion: "1.3"
