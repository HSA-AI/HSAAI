# Configuration template for Microsoft 365
apiVersion: hsaaiv2/v1
kind: IntegrationConfig
metadata:
  name: m365-config
  labels:
    integration: INT-M365
    category: productivity
    vendor: Microsoft
spec:
  enabled: true
  endpoint: "${INT-M365_ENDPOINT}"
  credentials:
    source: vault
    path: "secret/hsaai/integrations/m365"
  auth:
    type: oauth2
    clientId: "${INT-M365_CLIENT_ID}"
    clientSecret: "vault:m365/client-secret"
    tenant: "${INT-M365_TENANT}"
  pool:
    maxConnections: 20
    idleTimeout: 300s
  retry:
    maxRetries: 3
    backoff: exponential
    initialDelay: 1s
    maxDelay: 30s
  circuitBreaker:
    enabled: true
    failureThreshold: 5
    recoveryTime: 60s
  rateLimit:
    requestsPerSecond: 50
    burst: 100
  observability:
    metricsEnabled: true
    logsEnabled: true
    tracesEnabled: true
    logLevel: info
  security:
    piiDetection: true
    auditAll: true
    tlsMinVersion: "1.3"
