# Configuration template for Microsoft Teams
apiVersion: hsaaiv2/v1
kind: IntegrationConfig
metadata:
  name: teams-config
  labels:
    integration: INT-TEAMS
    category: productivity
    vendor: Microsoft
spec:
  enabled: true
  endpoint: "${INT-TEAMS_ENDPOINT}"
  credentials:
    source: vault
    path: "secret/hsaai/integrations/teams"
  auth:
    type: oauth2
    clientId: "${INT-TEAMS_CLIENT_ID}"
    clientSecret: "vault:teams/client-secret"
    tenant: "${INT-TEAMS_TENANT}"
  pool:
    maxConnections: 20
    idleTimeout: 300s
  retry:
    maxRetries: 3
    backoff: exponential
    initialDelay: 1s
    maxDelay: 30s
  circuitBreaker:
    enabled: true
    failureThreshold: 5
    recoveryTime: 60s
  rateLimit:
    requestsPerSecond: 50
    burst: 100
  observability:
    metricsEnabled: true
    logsEnabled: true
    tracesEnabled: true
    logLevel: info
  security:
    piiDetection: true
    auditAll: true
    tlsMinVersion: "1.3"
