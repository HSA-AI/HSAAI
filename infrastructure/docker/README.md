# HSAAI Docker Compose Files

Use the root-level docker-compose files:
- `docker-compose.dev.yml` — Development
- `docker-compose.production.yml` — Production
- `docker-compose.hsa-internal.yml` — Internal deployment (recommended)

The duplicate files in this directory have been removed to prevent drift.
