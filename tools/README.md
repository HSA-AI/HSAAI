# Dev CLI Tools (Reserved)

This directory is currently **empty** — the tools previously listed here
(`init_dev_env.py`, `seed_demo_data.py`, `generate_api_keys.py`,
`validate_project_structure.py`) never existed in the repository.
`validate_project_structure.py` actually lives in [`scripts/`](../scripts/).

FIXED (audit): the old README advertised four nonexistent tools. Do not
reference this directory from docs until real tools land.
