# AI Evaluation Suites

Currently this directory contains a single eval dataset:

- `arabic_enterprise_eval.json` — Arabic enterprise QA eval set
  (no automated runner yet; see also
  [`services/model_training`](../services/model_training) eval tooling and
  `services/backend_core/ai_quality/eval_pipeline.py`).

FIXED (audit): the old README advertised `rag/`, `agents/` and `llm/` suites
and runnable commands that never existed. Add suites with their runners, or
don't list them.
