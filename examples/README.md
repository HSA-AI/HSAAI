# API Usage Examples (Reserved)

This directory is currently **empty** — the example files previously listed
here (`python/…`, `typescript/…`, `curl/…`) never existed in the repository.

The fastest working examples today:
- Frontend: [`apps/web`](../apps/web) (login → chat → knowledge hub flows)
- API contracts: [`tests/contract/test_api_contracts.py`](../tests/contract/test_api_contracts.py)
