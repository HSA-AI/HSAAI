ENTERPRISE_INTEGRATION_SECURITY_RULES = [
    "Use LDAPS, HTTPS, Kerberos, OAuth2, or service-account tokens only.",
    "Never store SAP, AD, SMB, BI, HR, or ITSM secrets in code.",
    "Begin every integration in read-only mode until approved by governance.",
    "Map source-system permissions to HSAAI tenant/workspace permissions.",
    "Preserve Windows NTFS ACLs when indexing file-server documents.",
    "Record connector, user, tenant, workspace, time, and purpose in audit logs.",
]
