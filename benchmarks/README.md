# Performance Benchmarks (Reserved)

This directory is currently **empty** — the benchmark scripts previously
listed here (`rag_search.py`, `llm_generate.py`, `concurrent_users.py`,
`qdrant_cluster.py`) never existed in the repository.

For load testing use [`tests/load/`](../tests/load/) instead
(locust + k6 scripts live there).
