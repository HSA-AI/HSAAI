# HSAAI Test Requirements (v4.0)
# FIXED (audit: missing deps): tests/conftest.py builds an async SQLite engine
# (needs aiosqlite + sqlalchemy) and imports fastapi/pyyaml indirectly; CI's
# unit-tests job installs only this file, so the missing pins broke collection.
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.24
pytest-mock>=3.14
httpx>=0.27
# e2e: the `page` fixture comes from pytest-playwright (playwright alone does
# not register it — tests errored at collection whenever the stack was up).
pytest-playwright>=0.5
playwright>=1.45
testcontainers[postgres]>=4.7
locust>=2.27
bandit>=1.7
pip-audit>=2.7
# --- imports used by tests themselves / conftest ---
aiosqlite>=0.20
sqlalchemy>=2.0.30
pyyaml>=6.0
fastapi>=0.115
pydantic>=2.7

# Native isolated Redis test process.
redislite==6.2.912183
